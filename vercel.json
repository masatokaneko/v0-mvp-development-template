{
  "version": 2,
  "buildCommand": "pnpm install && pnpm approve-builds --all && pnpm prisma generate && pnpm run build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
