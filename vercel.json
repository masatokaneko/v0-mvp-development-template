{
  "version": 2,
  "buildCommand": "pnpm install && pnpm approve-builds && pnpm prisma generate && pnpm run build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
